import { Router } from "express"

const router = Router();


// Create a user if it doesn't exist,
// Generate email token and send it to email
router.post('/login', async(req, res) => {
    
})


//Validate the email token and exchange 

export default router;